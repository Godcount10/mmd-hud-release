# MMD HUD Release

MMD HUD 的公开编译产物仓库。源码保存在私有仓库中。

## 固定版本外链

```html
<script src="https://cdn.jsdelivr.net/gh/Godcount10/mmd-hud-release@v0.1.0/mmd-hud.js"></script>
```

每个版本均使用独立 Git tag；不要使用可变的 main 分支作为正式注入地址。
